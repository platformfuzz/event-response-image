# Optional: copy to .env and set for local Docker Compose testing.
# When set, FastSchema creates an admin user and the web app uses it for API auth.

# Admin user created in FastSchema and used by web app to call the content API
ADMIN_USER=admin
ADMIN_PASS=changeme

# Optional: fixed app key for FastSchema (default: server auto-generates)
# APP_KEY=
